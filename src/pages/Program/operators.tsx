import { Link } from "react-router-dom";

const Operators = () => {
    return (
        <div>
            <Link to="./observable">Observable</Link>
        </div>
    );
};

export default Operators;
